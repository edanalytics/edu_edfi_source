version: 2

models:
  - name: base_ef3__balance_sheet_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__chart_of_accounts
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__function_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__fund_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__local_accounts
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__local_actuals
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__local_budgets
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__local_contracted_staffs
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__local_encumbrances
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__local_payrolls
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__object_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__operational_unit_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__program_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__project_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"
  - name: base_ef3__source_dimensions
    config:
      tags: ['finance']
      enabled: "{{ var('src:domain:finance:enabled', False) }}"

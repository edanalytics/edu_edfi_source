version: 2

models:
  - name: cfg_student_assessment_score_name_dupes
    config:
      tags: ['assessment']
      enabled: "{{ var('src:domain:assessment:enabled', True) }}"
  - name: cfg_student_assessment_subjects
    config:
      tags: ['assessment']
      enabled: "{{ var('src:domain:assessment:enabled', True) }}"
  - name: cfg_student_objective_assessment_score_name_dupes
    config:
      tags: ['assessment']
      enabled: "{{ var('src:domain:assessment:enabled', True) }}"
